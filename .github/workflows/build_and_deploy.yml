name: Build and deploy
on:
  workflow_dispatch:
permissions:
  contents: write
  pages: write
  id-token: write
jobs:
  build:
    uses: netbeheer-nederland/build-dp/.github/workflows/publish.yml@main
